import React from 'react';

const CreateReport = () => {
  return (
    <div style={{ padding: '1rem' }}>
      <h3>Create Report</h3>
      <p>Report creation functionality will go here.</p>
      <div style={{ 
        border: '2px dashed #ccc', 
        padding: '2rem', 
        textAlign: 'center',
        borderRadius: '8px',
        marginTop: '1rem'
      }}>
        <p style={{ color: '#666' }}>Report creation interface placeholder</p>
      </div>
    </div>
  );
};

export default CreateReport;
